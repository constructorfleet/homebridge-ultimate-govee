{
  "pluginAlias": "Ultimate Govee",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "username": {
        "type": "string",
        "title": "Govee Account E-Email Address",
        "description": "Required for most device models and for AWS/BLE connections."
      },
      "password": {
        "type": "string",
        "title": "Govee Account Password",
        "description": "Required for most device models and for AWS/BLE connections."
      },
      "apiKey": {
        "type": "string",
        "title": "Govee API Key",
        "description": "Required to enable support for API devices. You can obtain your API key using <a href=\"https://github.com/bwp91/homebridge-govee/wiki/Configuration#obtaining-your-api-key\" target=\"_blank\">this guide</a>."
      }
    }
  }
}